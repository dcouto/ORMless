using Foundation.BaseClasses.MVC;
using Sitecore.Data.Items;

namespace Foundation.SharedContent.Models
{
    public class SharedContent : BaseModel
    {
        public SharedContent(Item item) : base(item)
        {
        }
    }
}