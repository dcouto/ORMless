using Foundation.BaseClasses.MVC;
using Sitecore.Data.Items;

namespace {{layer}}.{{name}}.Models
{
    public class {{name}} : BaseModel
    {
        public {{name}}(Item item) : base(item)
        {
        }
    }
}